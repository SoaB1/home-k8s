#cloud-config
hostname: master-0
timezone: Asia/Tokyo

# Username
users:
  - name: masaya
    groups: [adm, cdrom, sudo, dip, plugdev, lxd]
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    lock_passwd: true